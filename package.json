{
  "name": "tspp",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch run .tspp/run.ts",
    "once": "bun run .tspp/run.ts",
    "prod": "bun run .tspp/run.ts",
    "build": "bun run .tspp/build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-plugin-auto-imports": "^0.4.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
