{
  "name": "tspp",
  "type": "module",
  "private": true,
  "imports": {
    "#tspp/types": "./.tspp/types.ts"
  },
  "scripts": {
    "dev": "bun --watch run .tspp/run.ts",
    "once": "bun run .tspp/run.ts",
    "prod": "bun run .tspp/run.ts",
    "build": "bun run .tspp/build.ts",
    "format": "biome check --write .",
    "check": "biome check --config-path .tspp/biome.check.json . && eslint . && tsc --noEmit && bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.5",
    "@eslint/js": "^10.0.1",
    "@types/bun": "latest",
    "bun-plugin-auto-imports": "^0.4.1",
    "eslint": "^10.4.0",
    "globals": "^17.6.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.60.0"
  }
}
